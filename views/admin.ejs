<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css">
    <title><%= title %></title>
</head>
<body>
    <header class="site-header">
        <h1 class="site-title">Add New Lego Set</h1>
    </header>
    <main class="main-content">
        <% if (success) { %>
        <div class="success-message">
            <p><%= success %></p>
        </div>
        <% } %>
        <form action="/admin" method="POST" enctype="multipart/form-data" class="admin-form">
            <label for="id">Lego Set ID (5 digits):</label>
            <input type="number" id="id" name="id" required>

            <label for="name">Name:</label>
            <input type="text" id="name" name="name" required>

            <label for="year_released">Year Released:</label>
            <input type="number" id="year_released" name="year_released" required>

            <label for="pieces">Number of Pieces:</label>
            <input type="number" id="pieces" name="pieces" required>

            <label for="currentValue">Current Bid Price ($):</label>
            <input type="number" step="0.01" id="currentValue" name="currentValue" required>

            <label for="buy_it_now">Buy It Now Price ($):</label>
            <input type="number" step="0.01" id="buy_it_now" name="buy_it_now" required>

            <label for="images">Upload Images (up to 5):</label>
            <input type="file" id="images" name="images" accept="image/jpeg, image/png, image/webp" multiple required>

            <button type="submit" class="btn btn-primary">Add Lego Set</button>
        </form>
    </main>
    <footer class="site-footer">
        <p>&copy; 2024 Lego New In Box. All rights reserved.</p>
    </footer>
</body>
</html>
